# https://www.tensorflow.org/tensorboard/

tensorboard:
  _target_: rl4co.utils.loggers.FilteredTensorBoardLogger
  save_dir: "${paths.output_dir}/tensorboard/"
  name: null
  log_graph: False
  default_hp_metric: False
  prefix: ""
  # version: ""
